CREATE TABLE WindowSettings (
    characterId TEXT NOT NULL,
    name TEXT NOT NULL,
    open INTEGER NOT NULL,
    width INTEGER,
    height INTEGER,
    location TEXT,
    PRIMARY KEY (characterId, name)
);

INSERT INTO WindowSettings (characterId, name, open)
    SELECT characterId, name, 1
    FROM OpenWindow;

DROP TABLE OpenWindow;